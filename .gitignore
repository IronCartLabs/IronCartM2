# Composer
/.composer/
/vendor/
composer.phar

# IDE
/.idea/
/.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local test artifacts
/tests/_output/
/tests/_data/
/dev/tests/integration/tmp/

# Scan outputs
ironcart-scan*.json

# Env
.env
.env.local

# Local Magento sandbox (markshust/docker-magento harness — see docs/sandbox.md)
/.sandbox/

# Claude Code local state — never track
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Marketplace build artifacts — produced by `make build-marketplace`.
# The staged source tree, the composer-validate scratch space, and the
# released tarball all land here; none of it belongs in git.
/package-marketplace/build/
